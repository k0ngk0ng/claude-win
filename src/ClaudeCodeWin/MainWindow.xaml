<Window x:Class="ClaudeCodeWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Claude Code for Windows"
        Height="700" Width="1000"
        MinHeight="500" MinWidth="700"
        Background="{StaticResource BackgroundDark}"
        WindowStyle="SingleBorderWindow">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundMedium}" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo 和标题 -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🤖" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="Claude Code" FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Windows Client" FontSize="11"
                                       Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock x:Name="VersionText" FontSize="11" Margin="6,0,0,0"
                                       Foreground="{StaticResource TextSecondary}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- 工作目录选择 -->
                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <TextBlock Text="工作目录:" Foreground="{StaticResource TextSecondary}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="WorkingDirectoryBox"
                             Width="350"
                             Style="{StaticResource ModernTextBox}"
                             VerticalContentAlignment="Center"/>
                    <Button x:Name="BrowseButton" Content="..." Width="40" Margin="4,0,0,0"
                            Style="{StaticResource ModernButton}"
                            Click="BrowseDirectory_Click"/>
                </StackPanel>

                <!-- 操作按钮 -->
                <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="StartButton" Content="▶ 启动"
                            Style="{StaticResource ModernButton}"
                            Margin="0,0,8,0"
                            Click="StartButton_Click"/>
                    <Button x:Name="StopButton" Content="■ 停止"
                            Style="{StaticResource ModernButton}"
                            IsEnabled="False"
                            Margin="0,0,8,0"
                            Click="StopButton_Click"/>
                    <Button Content="⚙ 设置"
                            Style="{StaticResource ModernButton}"
                            Click="SettingsButton_Click"/>
                </StackPanel>

                <!-- 斜杠命令工具栏 -->
                <Border Grid.Row="1" Grid.ColumnSpan="3" Margin="0,10,0,0">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="SlashCommandsPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="快捷命令:" Foreground="{StaticResource TextSecondary}"
                                       VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12"/>
                            <Button Content="/help" Tag="/help" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="显示帮助信息" Click="SlashCommand_Click"/>
                            <Button Content="/clear" Tag="/clear" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="清除对话历史" Click="SlashCommand_Click"/>
                            <Button Content="/compact" Tag="/compact" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="压缩对话上下文" Click="SlashCommand_Click"/>
                            <Button Content="/resume" Tag="/resume" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="恢复上次会话" Click="SlashCommand_Click"/>
                            <Button Content="/memory" Tag="/memory" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="管理记忆" Click="SlashCommand_Click"/>
                            <Button Content="/mcp" Tag="/mcp" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="MCP 服务器管理" Click="SlashCommand_Click"/>
                            <Button Content="/config" Tag="/config" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="查看/修改配置" Click="SlashCommand_Click"/>
                            <Button Content="/cost" Tag="/cost" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="显示 token 消耗" Click="SlashCommand_Click"/>
                            <Button Content="/doctor" Tag="/doctor" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="检查健康状态" Click="SlashCommand_Click"/>
                            <Button Content="/init" Tag="/init" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="初始化项目" Click="SlashCommand_Click"/>
                            <Button Content="/review" Tag="/review" Style="{StaticResource ModernButton}"
                                    FontSize="11" Padding="6,3" Margin="2,0"
                                    ToolTip="代码审查" Click="SlashCommand_Click"/>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <!-- 终端区域 -->
        <Border Grid.Row="1" Margin="10"
                Background="{StaticResource BackgroundMedium}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="4">
            <wv2:WebView2 x:Name="TerminalWebView"
                          DefaultBackgroundColor="#1a1a2e"/>
        </Border>
    </Grid>
</Window>
