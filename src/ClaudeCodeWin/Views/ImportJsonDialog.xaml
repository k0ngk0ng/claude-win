<Window x:Class="ClaudeCodeWin.Views.ImportJsonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导入 JSON 配置"
        Height="400" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundDark}"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="粘贴 JSON 配置"
                       FontSize="18" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="支持直接粘贴环境变量 JSON 或包含 env 字段的配置"
                       Foreground="{StaticResource TextSecondary}"
                       FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- JSON 输入框 -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundMedium}"
                CornerRadius="4" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <TextBox x:Name="JsonTextBox"
                     Background="Transparent"
                     Foreground="{StaticResource TextPrimary}"
                     FontFamily="Consolas"
                     FontSize="12"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Padding="10"
                     BorderThickness="0"/>
        </Border>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="取消" Width="80"
                    Style="{StaticResource ModernButton}"
                    Background="{StaticResource BackgroundLight}"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button Content="导入" Width="80"
                    Style="{StaticResource ModernButton}"
                    Click="ImportButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
